# =============================================================================
# Titan Habitability Pipeline – Python Dependencies
# =============================================================================
# Install:  pip install -r requirements.txt
#
# Python version
# --------------
#   MINIMUM : Python 3.10
#     Driven by xarray>=2024.1 (drops 3.9) and pymc>=5.10 (drops 3.9).
#     If you comment out pymc/pytensor AND pin xarray<2024.1, the minimum
#     drops to Python 3.9 (sklearn-only mode).
#
#   MAXIMUM : Python 3.12  (all wheels verified)
#     Python 3.13 (released Oct 2024) should work but the full geospatial
#     stack (rasterio + cartopy + pymc) has not yet been broadly validated
#     against it. Pin to <=3.12 for reproducible environments.
#
#   Recommended: Python 3.11 or 3.12
#
# GDAL note (macOS)
# -----------------
#   brew install gdal
#   pip install gdal==$(gdal-config --version)
#
# Docker (GDAL already present in your image):
#   FROM ghcr.io/osgeo/gdal:ubuntu-small-latest
#   RUN pip install -r requirements.txt
#
# Bayesian backends
# -----------------
#   Default : scikit-learn   (always installed below)
#   Optional: pymc           (uncomment block to enable)
#   Optional: numpyro + jax  (uncomment block to enable)
# =============================================================================

# ── Core scientific stack ────────────────────────────────────────────────────
numpy>=1.26,<3.0
scipy>=1.12
pandas>=2.2
pyarrow>=14.0          # parquet I/O for VIMS spatial-footprint index

# ── Geospatial / GIS ─────────────────────────────────────────────────────────
rasterio>=1.3          # GeoTIFF read/write (requires GDAL C-binaries)
pyproj>=3.6            # CRS transforms; Titan sphere PROJ strings
shapely>=2.0           # geometry operations
geopandas>=0.14        # shapefile I/O for Lopes et al. geomorphology layers
xarray>=2024.1         # multi-dim labelled arrays + NetCDF
netCDF4>=1.6           # CIRS / INMS atmospheric NetCDF files
h5py>=3.10             # HDF5 support
astropy>=6.0           # FITS I/O, PDS WCS utilities

# ── VIMS cube processing ──────────────────────────────────────────────────────
# pyvims>=2.0          # Cassini VIMS .qub cube reader
#                      # Install separately: pip install pyvims
#                      # https://github.com/seignovert/pyvims
#                      # Required only for raw spectral-cube ingest;
#                      # not needed if using the pre-built VIMS parquet index.

# ── Data acquisition ──────────────────────────────────────────────────────────
requests>=2.31
tqdm>=4.66             # download progress bars for large Cassini archives
pooch>=1.8             # reproducible dataset downloads with SHA-256 verification

# ── Bayesian inference ────────────────────────────────────────────────────────
scikit-learn>=1.4      # DEFAULT backend: GaussianNB + isotonic calibration
arviz>=0.18            # posterior diagnostics, HDI plots (all backends)

# PyMC backend (optional – comment out if not needed)
pymc>=5.10
pytensor>=2.18

# NumPyro backend (optional – fast, JAX-based)
# numpyro>=0.13
# jax>=0.4.25
# jaxlib>=0.4.25

# ── Image processing ──────────────────────────────────────────────────────────
Pillow>=10.2
scikit-image>=0.22

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.8
cartopy>=0.23          # planetary map projections (Mollweide, Robinson, etc.)
seaborn>=0.13
plotly>=5.20           # interactive HTML output figures

# ── Notebook / reporting ──────────────────────────────────────────────────────
jupyter>=1.0
nbformat>=5.9
ipywidgets>=8.1

# ── Utilities ─────────────────────────────────────────────────────────────────
typing-extensions>=4.10
pyyaml>=6.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0
